import React from 'react'
import { Card, Typography } from 'antd'

const { Title } = Typography

const SpaceDetailPage: React.FC = () => {
  return (
    <div className="p-6">
      <Card>
        <Title level={2}>空间详情</Title>
        <p>空间详情页面正在开发中...</p>
      </Card>
    </div>
  )
}

export default SpaceDetailPage